// import { CoordinatesController } from "../controllers/coordinates.controller";

// const io = jest.mock("socket.io").;

// const GeoService = jest.mock("../services");

// beforeEach(() => {
//   // Clear all instances and calls to constructor and all methods:
//   GeoService.clearAllMocks();
// });

// describe("Coordinates Controller", () => {
//   it("should register handlers", () => {
//     const Mock = jest.fn(() => ({
//       registerHandlers: jest.fn(),
//     }));

//     const mock = new Mock();

//     // @ts-ignore
//     const instance = new CoordinatesController(io, io.socket);

//     instance.registerHandlers();
//     expect(mock).toBeCalled();
//   });
// });
